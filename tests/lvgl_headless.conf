# The suites only exercise the widget configuration layer, so LVGL is built
# without binding to a display: enabling CONFIG_DISPLAY would drag in SDL on
# native_sim and make Zephyr launch the QEMU targets with "-display sdl", which
# not every SDK QEMU build accepts.
CONFIG_LV_Z_AUTO_INIT=n
